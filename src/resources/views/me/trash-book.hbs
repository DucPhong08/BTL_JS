<title>Truyện Của Tôi Đăng</title>
<div class="mt-4 mx-5 " style="height: 500px;">
    <h2>
        <a href="/">Trang Chủ</a> / Thùng rác
    </h2>
    <a href="/me/stored/{{user._id}}/book" class="btn-link ">Truyện Của Tôi Đăng</a>
    <table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Tên Truyện</th>
            <th scope="col">Số Chương</th>
            <th scope="col">Thời Gian Xoá</th>
            <th scope="col" colspan="2">Thao Tác</th>
        </tr>
    </thead>
    <tbody>
        {{#each book}}
        <tr>
            <th scope="row">{{sum @index 1}}</th>
            <td>{{this.name}}</td>
            <td>{{this.Chapter}}</td>
            <td>{{time this.deletedAt}}</td>
            <td>
                <a href="" class="btn btn-link btn-restore" data-id ="{{this.slug}}">Khôi Phục</a>
                <a href="" class="btn btn-link" data-toggle="modal" data-id ="{{this.slug}}" data-target="#delete-book-modal">Xoá Vĩnh Viễn</a>

            </td>
        </tr>
        {{else}}
        <tr>
          <td colspan="5">
            <div class="alert alert-warning text-center">
                Thùng rác trống .<span class="mx-1"></span>
                <a href="/me/stored/book" class="btn-link">Danh sách Truyện</a>
            </div>
          </td>
        </tr>
        {{/each }}
    </tbody>
</table>
</div>
{{!-- confirm delete --}}
<div class="modal fade" id="delete-book-modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Thông báo</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        Hành động này không thể khôi phục . Bạn vẫn muốn xoá book ? 
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-primary" id="btn-delete-book">XOÁ VĨNH VIỄN</button>
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Huỷ</button>
      </div>
    </div>
  </div>
</div>
<form name="delete-book-form" method="POST" ></form>
<form name="restore-book-form" method="POST" ></form>



<script>
    document.addEventListener("DOMContentLoaded", function(){
        var bookID;
        var deleteForm = document.forms['delete-book-form'];
        var restoreForm = document.forms['restore-book-form'];
        var btndelete = document.getElementById('btn-delete-book');
        var restoreBtn = $('.btn-restore') 
        $('#delete-book-modal').on('show.bs.modal', function (event) {
            var button = $(event.relatedTarget) 
            bookID = button.data('id') 
            
        })
        
        btndelete.onclick = function(){
            deleteForm.action = '/book/' + bookID + '/force';

            deleteForm.submit();
        }
        restoreBtn.on('click', function(e){
          e.preventDefault();
          var  bookID = $(this).data('id');
          restoreForm.action = '/book/' + bookID + '/restore?_method=PATCH'
            restoreForm.submit();
        });
    })
</script>